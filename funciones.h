
    #include <stdio.h>

    /** \brief Se realiza la operacion sumar entre dos numeros ingresados
     *
     * \param A , primer valor a sumar
     * \param B, segundo valor a sumar
     * \return  Nos devuelve un numero entero
     *
     */
    int SumarNumeros(int ,int );

    /** \brief Se realiza la operacion resta entre dos numeros ingresados
     *
     * \param A, primer valor a restar
     * \param  B, segundo valor a restar
     * \return Nos devuelve un numero entero
     *
     */
     int RestarNumeros(int ,int );

    /** \brief Se realiza la operacion multiplicacion entre dos numeros ingresados
     *
     * \param A, primer valor a multiplicar
     * \param  B, segundo valor a multiplicar
     * \return  Nos devuelve un numero entero
     *
     */
    int MultiplicarNumeros(int ,int );

    /** \brief Se realiza la operacion division entre dos numeros ingresados
     *
     * \param A, primer valor a dividir
     * \param B, segundo valor a dividir
     * \return  Nos devuelve un numero flotante
     *
     */

    float DividirNumeros(float ,int );
    /** \brief Se realiza la operacion factorial entre dos numeros ingresados
     *
     * \param  A ,primer valor para factorizar
     * \param  B ,segundo valor para factorizar
     * \return long int Nos devuelve un numero entero
     *
     */
    long int FactorizarNumero(int);
